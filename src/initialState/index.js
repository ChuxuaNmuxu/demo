const initialState = {
    homeReducers: {
        todos: [{
            text: '拯救大兵',
            completed: false
        }],
    },
    dataReducers: {
        fetchData: []
    }
};

export default initialState;
